services:
  - type: web     
    name: Frontkick 
    env: node              
    branch: main           
    buildCommand: "npm install && npm run build"
    publishPath: "dist/frontkick"  
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
